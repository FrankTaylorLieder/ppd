[package]
name = "mme-display"
version = "0.1.0"
edition = "2021"

[dependencies]
pyportal = { version = "0.11", features = ["rt", "display"] }
cortex-m = { version = "0.7", features = ["critical-section-single-core"] }
embedded-graphics = "0.8"
panic-halt = "1.0"

[profile.release]
debug = true
